# 临时文件（可随时清空，归档时剔除；保留本区索引 README）
process/tmp/*
!process/tmp/README.md

# 常见运行时产物
node_modules/
__pycache__/
.venv/
venv/
dist/
*.pyc
.DS_Store
Thumbs.db

# 敏感信息（绝不入库，见组级 standards.md §2）
# 注意：`*.env` 只匹配以 .env 结尾者，故 .env.template 不受影响、可入库；
# 真实密钥文件为 .env（被本条匹配）。写法用「目录/文件」精确路径，避免裸名跨深度误伤。
*.env
secrets/
credentials/
*.pem
*token*
process/project/configs/project-config.toml

# 项目内技能目录 .agents/ 不设忽略规则：它是项目资产，默认入库。
# （旧规则 `.dsh/*` + `!.dsh/skills/` 已删：`.dsh` 是用户级 harness home 的目录名，
#   项目级再用它会把项目资产与工具状态混在同一名下；那条规则还会把 skills 以外的
#   文件静默排除出 git，见 AGENTS.md 变更记录 v1.2。）
