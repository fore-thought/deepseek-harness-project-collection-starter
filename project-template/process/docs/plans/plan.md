# 项目计划书（plan）

> 规划闸产物（组级 AGENTS.md §7.2）：/plan 模式编写，经用户批准后执行。
> 格式参考 `process/templates/plans/plan.template.md`（实例骨架与模板同步）。
> 读取方式（组级 AGENTS.md §5）：**分段读**——只读当前段的完整门禁表，已完成段看折叠摘要。

## 0. 一页概览

- **做什么**：（待填写）
- **为什么**：（待填写）
- **第一成果**：（待填写）
- **预算/时间**：（待填写）
- **如何验收**：每个里程碑有明确验收标准（见 §2），不达标不进入下一阶段。

## 1. 背景与约束

- （待填写）

## 2. 里程碑与验收标准

| 里程碑 | 内容 | 验收标准（全部满足才通过） | 预算 |
|---|---|---|---|
| （待填写） |  |  |  |

## 3. 门禁表（必填，每行 4 列）

| 触发条件 | 通过凭证 | 通过后做什么 | 未通过默认动作（停止+卡片请示） |
|---|---|---|---|
| （待填写） |  |  |  |
| 门禁表全部行完成（计划耗尽） | 目标书「成功判定」逐条打勾的结论 + 用户卡片答复 | 全打勾 → 结项或开新目标书（回立项闸）；未全打勾 → 提交计划书增补并获批准后回执行闸 | 停止 + 卡片请示 |

> **分段与折叠**：本表按里程碑分段；已完成的段折叠成一行
> `里程碑 N：已完成 → 见 decisions 的 DECISION-XXX`，只让当前段保持完整 4 列。

## 4. 工作单元切分

- 单元定义格式固定："一个可验收产出"
- 单元列表：（待填写）
- 若与其他会话并行：每个单元注明预期触碰的文件与目录集合（组级 AGENTS.md §6）
- 单元命名：`u<序号>-<kebab-case 主题>`（分支 = 单元名；会话名 = `<单元名>-<YYYYMMDD>`）

## 5. 风险、合规与规范

| 风险 | 等级 | 应对 |
|---|---|---|
| （待填写） |  |  |

## 6. 待用户决策事项

1. （待填写）

## 7. 执行纪律

- 严格执行本计划，**不主动降级任务目标**；
- 每阶段结束做验收汇报（对照验收标准逐条打勾）；
- 全程保持 [MEASURED]/[ESTIMATED]/[UNKNOWN] 分层，新数字先登记假设再引用。
