> 临时垃圾文件：调试脚本、中间导出、缓存；可随时清空，
> 归档/打包时整体剔除。已被 `.gitignore` 排除，不入库。
>
> **多角色模式下的特殊地位：本区无归属，任何角色都能写。**
> 它是"命令产文件"的合法落点——跑构建、测试、脚本、下载时产生的文件一律落在这里，
> 不得落进别人的命名空间（组级 `pipeline-and-roles.md` §1、§3.1 第二档）。
