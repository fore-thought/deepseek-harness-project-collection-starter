> 配置：训练配置、流水线参数等。
> 项目配置模板：`project-config.toml.template` → 复制为 `project-config.toml`（用户填写，不入库）。
> 密钥模板：`.env.template` → 复制为 `.env`（用户填写，不入库；密钥只存于此）。
> 命名规则：模板名 = 实例名 + `.template`，去掉后缀即实例名。
> 详见组级 AGENTS.md §6 / standards.md §2。
