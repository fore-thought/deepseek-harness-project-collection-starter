# 环境变量模板（占位符版，可入库）—— 真实文件为 .env（.gitignore 排除，不入库）
# 使用：复制本文件为 .env 并填写真实值；密钥只存于此（project-config.toml 不列密钥）
# 命名规则：模板名 = 实例名 + `.template`，去掉后缀即实例名（组级 AGENTS.md §6）。
# 键名约定：<PROVIDER>_<字段>，全大写 + 下划线；PROVIDER 用平台官方英文名，
#   字段名逐字取自该平台官方文档的凭证字段名（不凭记忆转写，见组级 AGENTS.md §2.6）。
#   示例形态：<PROVIDER>_CLIENT_ID / <PROVIDER>_CLIENT_SECRET / <PROVIDER>_ACCESS_TOKEN
# 本模板不预置任何具体平台的键名——那是单个项目的渠道清单，不属于模板。
<PROVIDER>_CLIENT_ID=<client_id>
<PROVIDER>_CLIENT_SECRET=<client_secret>
