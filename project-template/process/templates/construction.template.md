# 项目施工文档（construction 模板）

> 模板位置：本文件是**模板**，永不改内容；**实例** = 复制为 `process/docs/construction.md`
> （去掉 `.template` 后缀）后填写。
> **本文件 = 交付版施工文档**：干净、只记成功步骤（错改对记一步到位）、
> **换个 AI/换目录能从空文件夹复原当前项目进度**。
> ⚠️ **与 `process/docs/work-log.md`（每日实际施工日志，全路径，含反复修改/失败）分工不同**，两者勿混。
> 读取方式（组级 AGENTS.md §5）：**按步骤清单执行**——只留能照着跑的东西，叙述性文字
> 移到 `process/project/` 下对应说明文件，本文件不写叙述。

> 原则：标注 [MEASURED]（实测）/ [ESTIMATED]（估算）。选型一律主流成熟方案。

## 0. 目标产物

`<一句话：本流水线最终产出什么>`

## 1. 硬件/系统前置

- `<硬件/系统/网络前置条件>`

## 2. 目录结构（与仓库一致）

```
<目录树>
```

## 3. 步骤一：`<环境搭建>`

> 每步固定三行：前置条件 / 命令或脚本 / 通过判据。

- 前置条件：`<硬件/依赖/配置项>`
- 命令或脚本：`<process/project/scripts/xxx.ext，或一条可直接执行的命令>`
- 通过判据：`<非零退出码即失败；成功时输出什么>`

## 4. 步骤二：`<……>`

- 前置条件：`<……>`
- 命令或脚本：`<……>`
- 通过判据：`<……>`

## N. 验证（验收标准）

- `<产出物与验收对照>`

## 通用注意事项

- 大文件下载一律用户在主机执行（代理列下载表：网址+存储路径）
- 里程碑成果 → 第一时间总结 → 提交并推送到远端仓库
