# 项目计划书（plan 模板）

> 模板位置：本文件是**模板**，永不改内容；**实例** = 复制为 `process/docs/plans/plan.md`
> （去掉 `.template` 后缀）后填写。
> 用途：规划闸产物（项目组级规范 ../../../../shared-standards/AGENTS.md §7.2）。
> 在 /plan 模式下编写，经用户批准后执行。

## 0. 一页概览

- **做什么**：`<一句话>`
- **为什么**：`<一句话>`
- **第一成果**：`<首个可验收产出>`
- **预算/时间**：`<预算与时间承诺>`
- **如何验收**：每个里程碑有明确验收标准（见 §2），不达标不进入下一阶段。

## 1. 背景与约束

- 定位/硬约束/资源边界：`<列出>`

## 2. 里程碑与验收标准

| 里程碑 | 内容 | 验收标准（全部满足才通过） | 预算 |
|---|---|---|---|
|  |  |  |  |

## 3. 门禁表（必填，每行固定 4 列）

> 格式定义与示例（示例随格式同步更新，见组级 AGENTS.md §7.2）：

| 触发条件 | 通过凭证 | 通过后做什么 | 未通过默认动作（停止+卡片请示） |
|---|---|---|---|
| 单元 U1 验收 | 门禁逐条打勾 + 成果入 work-log/construction + commit/push + 用户确认"通过" | 进入 U2 | 停止 + 卡片请示（修改或重做） |
| 里程碑 1 结束 | 验收标准逐条打勾 + 决策回写 + 复盘纪要 + 总结推送 | 进入里程碑 2 | 停止 + 卡片请示 |

## 4. 工作单元切分

- 单元定义格式固定："一个可验收产出"
- 单元列表：`<单元 1 / 单元 2 / …>`

## 5. 风险、合规与规范

| 风险 | 等级 | 应对 |
|---|---|---|
|  |  |  |

## 6. 待用户决策事项

1. `<决策点>`

## 7. 执行纪律

- 严格执行本计划，**不主动降级任务目标**；
- 每阶段结束做验收汇报（对照验收标准逐条打勾）；
- 被驳回的请求 → 想备用方案再提；
- 全程保持 [MEASURED]/[ESTIMATED]/[UNKNOWN] 分层，新数字先登记假设再引用。
