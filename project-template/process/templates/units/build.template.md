# 单元施工记录（build 模板）

> 模板位置：本文件是**模板**，永不改内容；**实例** = 复制为
> `process/docs/units/<单元名>/build.md`（去掉 `.template` 后缀）后填写。
> **写者：实现者独占。** 子代理与同角色的并发会话各写 `build-part<N>.md`，
> 由实现者的下一次交接汇总进本文件。
> ⚠️ 与 `process/docs/work-log.md` 分工不同：这里只记**本单元**的施工；
> 共享日志由制作人写。

状态：`施工中` ｜ 交底版本：`<brief.md / brief-v2.md>`

## 1. 做了什么

- `<按交底逐条对照，写明实际做法>`

## 2. 产物清单

| 产物 | 路径 | 说明 |
|---|---|---|
|  |  |  |

## 3. 对照验收细则的自检

| # | 判据 | 自检结果 | 证据 |
|---|---|---|---|
| 1 |  |  |  |

## 4. 实际触碰的路径

- `<列出实际改动路径，供验收员核对是否越出交底声明的文件集>`

## 5. 候选决策（交给制作人升格）

> 实现中发现的方向性/口径性问题写在这里，**不要直接去写 `decisions.md`**——
> 那份文件由制作人独占，由他判断哪些值得升格成 ADR。

- `<事项 + 为什么值得记 + 建议>`

## 6. 未解决 / 偏离

- `<偏离交底之处与原因；未解决的阻塞>`
