# 单元交底（brief 模板）

> 模板位置：本文件是**模板**，永不改内容；**实例** = 复制为
> `process/docs/units/<单元名>/brief.md`（去掉 `.template` 后缀）后填写。
> **写者：制作人独占。** 交出后不再修改；要改就出新版本文件或追加 `brief.addendum.md`。
> 状态标记：把下面第一行的状态改成 `已交底` 之后，实现者才可以开始施工。

状态：`草稿` ｜ 交底日期：`<YYYY-MM-DD>`

## 1. 这个单元要产出什么

`<一句话：一个可验收产出>`

## 2. 验收细则（实现者与验收员都照这个做）

| # | 判据（可打勾） | 怎么验（命令/证据） |
|---|---|---|
| 1 | `<判据>` | `<命令或证据路径>` |
| 2 |  |  |

## 3. 预期触碰的文件集

> 用途有两个：判定两个单元能否并行（文件集不相交才可并行），以及验收时核对
> "实际变更路径是否都在这个集合内"。

- 写入：`<路径或前缀>`
- 只读参考：`<路径>`

## 4. 前置条件与依赖

- `<依赖的单元/文档/用户提供的材料>`

## 5. 边界（明确不做的事）

- `<排除项>`
