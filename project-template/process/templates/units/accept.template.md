# 单元验收报告（accept 模板）

> 模板位置：本文件是**模板**，永不改内容；**实例** = 复制为
> `process/docs/units/<单元名>/accept.md`（去掉 `.template` 后缀）后填写。
> **写者：验收员独占。** 不改动被验收的产物，也不改计划书与共享日志。

状态：`待验收 / 已通过 / 已驳回` ｜ 验收日期：`<YYYY-MM-DD>`
验收对象：`build.md`（及其 `build-part*.md`） ｜ 依据：`brief.md` 的验收细则

## 1. 逐条结论

| # | 判据 | 结论 | 证据（命令/输出/产物路径） |
|---|---|---|---|
| 1 |  | 通过 / 不通过 |  |

## 2. 路径越界核对

> 核对"实际变更路径"是否都在交底声明的**预期触碰文件集**之内。这是越界的主要发现手段。
> **本节就是验收员记偏差的地方**：发现越界只在 `accept.md` 里写一行，不另开文件；
> 制作人里程碑复盘时汇总（组级 `pipeline-and-roles.md` §3）。

- 结论：`无越界 / 有越界（列出）`

## 3. 复现方式

- `<验收员怎么复现的：命令、脚本路径、环境>`
- 复现命令产生的文件落在哪里：`process/tmp/`（默认）。
  **不得落在 `outputs/` 或 `process/project/`**——取证不该改变被取证的东西（§3.1 第二档）。

## 4. 结论与后续

- 总结论：`通过 / 需修改（列出必须修的点）`
- 交回制作人的事项：`<需要制作人裁决或排期的点>`
