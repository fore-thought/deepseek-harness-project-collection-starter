# 角色交接文件（role-handoff 模板）

> 模板位置：本文件是**模板**，永不改内容；**实例** = 复制为
> `process/docs/handoff/<角色>/<角色>-<序号>-<YYYYMMDD>.md`（去掉 `.template` 后缀）后填写。
> 每个角色一份索引（`<角色>/index.md`），每次交接**追加一行**指向本文件；本文件本身
> **不覆盖旧的**。这样每次交接都是新增，覆盖竞争从物理上消失。

角色：`制作人 / 实现者 / 验收员` ｜ 序号：`<NN>` ｜ 日期：`<YYYY-MM-DD>`

## 1. 本会话完成了什么

- `<里程碑性成果，逐条>`

## 2. 当前接续点

- 正在做的单元：`<单元名>`
- 做到哪一步：`<交底已交 / 施工中 / 待验收 / 已验收>`
- 下一个动作：`<具体到"读哪个文件、写哪个文件">`

## 3. 挂起事项与等待的决策点

- `<等用户拍板的点；跨会话无回复时从这里恢复>`

## 4. 本角色的注意事项

- `<本角色特有的坑、约定、踩过的雷>`

## 5. 给下一个会话的第一句话

```
<可复制的文本>
```
