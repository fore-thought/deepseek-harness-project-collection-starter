# 决策记录（decisions 模板）

> 模板位置：本文件是**模板**，永不改内容；**实例** = 复制为 `process/docs/decisions.md`
> （去掉 `.template` 后缀）后填写。
> ADR 风格：记录关键工程/方向决策的**背景、选项、选择与理由**，供后续会话回溯。
> 读取方式（组级 AGENTS.md §5）：**索引 + 按需展开**——接续闸只读索引区，正文按需展开。
> 每个决策独立编号，新决策追加在正文区末尾（"重大决策"判定见组级 AGENTS.md §6 A2）。

## 索引区（接续闸只读到这里）

| ID | 日期 | 决策 | 状态 |
|---|---|---|---|
| DECISION-001 | `<日期>` | `<一句话>` | ✅ 已批准 |

> 索引区到此为止；下面才是正文。**只追加、不重写、不重排**，保证并发合并时能按时间戳解决。

---

## 正文区

### DECISION-XXX｜日期

- 背景：为什么需要决策（关联假设 ID [ASSUMPTION-XXX]）
- 选项：A / B / C（各一句）
- 决策：选哪个，为什么
- 代价/风险：放弃的选项与后续注意
- 影响：哪些计划/文件/假设被改变

## 待决策事项（当前已知）

- `<待决策项>`
